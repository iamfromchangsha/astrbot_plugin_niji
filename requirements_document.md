# Niji Diary Sync 插件需求文档

## 1. 项目概述

### 1.1 项目名称
Niji Diary Sync

### 1.2 项目描述
自动将用户与 AI 的聊天记录同步至 [你的日记](https://nijiweb.cn) 网站，支持每日自动上传和手动触发同步，打造属于你的私人 AI 对话存档。

### 1.3 版本要求
- **AstrBot ≥ v4.9.2**
- **Python ≥ 3.8**
- 网络可访问 `https://nijiweb.cn`

## 2. 功能需求

### 2.1 核心功能

| 功能 ID | 功能名称 | 功能描述 | 优先级 |
|--------|---------|---------|--------|
| F1 | 账号绑定 | 用户可通过 `/login` 命令绑定"你的日记"账号 | 高 |
| F2 | 对话记录 | 自动记录用户发送的消息和 AI 的回复 | 高 |
| F3 | 每日自动上传 | 每天北京时间 23:50 自动将当日新增对话上传至"你的日记" | 高 |
| F4 | 增量上传 | 智能检测新增消息，仅上传未同步的内容 | 高 |
| F5 | 手动同步 | 支持使用 `/sync` 命令手动触发上传，`/sync force` 强制重新上传 | 中 |
| F6 | 日记管理 | 若当天已有同名日记，将新对话更新到该日记中 | 中 |
| F7 | 多用户支持 | 支持多用户独立记录，互不干扰 | 中 |

### 2.2 辅助功能

| 功能 ID | 功能名称 | 功能描述 | 优先级 |
|--------|---------|---------|--------|
| F8 | 仅记录文本 | 忽略图片、语音、表情包等非文本消息 | 中 |
| F9 | 上传失败处理 | 若网络异常导致上传失败，数据会保留并在下次同步时重试 | 低 |

## 3. 技术需求

### 3.1 技术栈
- **开发语言**：Python 3.8+
- **依赖库**：
  - `aiohttp>=3.9.0`（用于 HTTP 请求）
- **核心 API**：AstrBot 插件 API

### 3.2 数据结构

| 数据结构 | 描述 | 存储方式 |
|---------|------|----------|
| 用户数据 | 存储用户登录"你的日记"后的令牌、用户ID、同步状态等 | 本地 JSON 文件 |
| 对话记录 | 从 AstrBot 对话管理器获取的聊天记录 | 内存（运行时获取） |

### 3.3 API 接口

| API 接口 | 方法 | 功能描述 |
|---------|------|----------|
| `https://nijiweb.cn/api/login/` | POST | 用户登录，获取令牌 |
| `https://nijiweb.cn/` | GET | 获取用户信息和日记列表 |
| `https://nijiweb.cn/api/` | POST | 发布或更新日记 |

## 4. 实现细节

### 4.1 插件注册
- 使用 `@register` 装饰器注册插件
- 插件名称：`NijiDiarySync`
- 插件描述：日记同步助手
- 插件版本：1.4.0
- 插件主页：https://github.com/your-name/astrbot_plugin_niji

### 4.2 命令处理
- `/login <邮箱> <密码>`：绑定"你的日记"账号
- `/sync`：手动触发今日新增内容的上传
- `/sync force`：强制重新上传今日全部聊天记录

### 4.3 事件监听
- 无需主动监听消息事件，通过对话管理器获取历史记录
- 仅记录文本内容，忽略非文本消息

### 4.4 调度器
- 每天北京时间 23:50 触发上传
- 使用信号量限制并发上传数，避免触发网站限流

### 4.5 上传逻辑
1. 检查用户是否已绑定
2. 获取完整对话记录
3. 根据同步状态计算新增消息
4. 格式化对话内容
5. 检查当天是否已有同名日记
6. 若有，更新该日记；若无，创建新日记
7. 上传成功后更新同步状态

### 4.6 数据持久化
- 用户数据存储在本地 JSON 文件中
- 路径：`data/plugin_data/astrbot_plugin_niji/niji_users.json`
- 插件启动时加载数据，状态变更时保存数据

## 5. 验收标准

| 验收项 | 测试方法 | 预期结果 |
|-------|---------|----------|
| 账号绑定 | 发送 `/login <邮箱> <密码>` | 返回绑定成功提示，用户数据被保存 |
| 自动上传 | 等待 23:50 或修改代码触发时间 | 对话被上传至"你的日记"，同步状态更新 |
| 手动同步 | 发送 `/sync` 命令 | 当日新增对话被上传 |
| 强制同步 | 发送 `/sync force` 命令 | 当日全部对话被重新上传 |
| 多用户支持 | 多个用户绑定账号并发送消息 | 各自的对话被独立记录和上传 |
| 增量上传 | 多次发送消息并触发同步 | 仅上传新增消息，避免重复 |
| 日记更新 | 一天内多次触发同步 | 所有消息被更新到同一篇日记 |

## 6. 风险评估

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| 网络异常 | 上传失败 | 数据保留，下次同步时重试 |
| 网站结构变更 | 登录或上传失败 | 增加错误处理和日志记录 |
| 账号密码错误 | 绑定失败 | 返回错误提示，引导用户检查 |
| 并发上传过多 | 触发网站限流 | 使用信号量限制并发数 |

## 7. 开发计划

1. **需求分析**：确认功能需求和技术要求
2. **代码实现**：根据需求文档实现插件代码
3. **测试验证**：测试所有功能是否正常工作
4. **文档更新**：更新 README.md 和相关文档
5. **部署发布**：提供安装和使用说明

## 8. 总结

Niji Diary Sync 插件旨在为用户提供一个自动、安全、便捷的 AI 对话存档解决方案。通过每日自动同步和手动触发同步，用户可以轻松管理与 AI 的对话记录，打造属于自己的私人 AI 对话历史库。插件设计简洁，功能明确，易于使用和维护。