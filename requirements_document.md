# Niji Diary Logger 插件需求文档

## 1. 项目概述

### 1.1 项目名称
Niji Diary Logger

### 1.2 项目描述
自动将用户与 AI 的完整对话记录（用户消息 + AI 回复）每日同步至 [你的日记](https://nijiweb.cn) 网站，打造属于你的私人 AI 对话存档。

### 1.3 版本要求
- **AstrBot ≥ v4.9.2** （必须 ≥ 4.9.2，因使用了 `get_all_kv_keys()` API）
- **Python ≥ 3.8**
- 网络可访问 `https://nijiweb.cn`

## 2. 功能需求

### 2.1 核心功能

| 功能 ID | 功能名称 | 功能描述 | 优先级 |
|--------|---------|---------|--------|
| F1 | 账号绑定 | 用户可通过 `/login` 命令绑定"你的日记"账号 | 高 |
| F2 | 对话记录 | 自动记录用户发送的消息和 AI 的回复 | 高 |
| F3 | 每日自动上传 | 每天北京时间 23:50 自动将当日对话上传至"你的日记" | 高 |
| F4 | 日记管理 | 若当天已有日记，将新对话追加到末尾（用 `---` 分隔） | 中 |
| F5 | 多用户支持 | 支持多用户独立记录，互不干扰 | 中 |

### 2.2 辅助功能

| 功能 ID | 功能名称 | 功能描述 | 优先级 |
|--------|---------|---------|--------|
| F6 | 仅记录文本 | 忽略图片、语音、表情包等非文本消息 | 中 |
| F7 | 上传失败处理 | 若网络异常导致上传失败，数据会保留并在次日重试 | 低 |
| F8 | 账号解绑 | 提供 `/logout` 命令用于解绑账号 | 低 |

## 3. 技术需求

### 3.1 技术栈
- **开发语言**：Python 3.8+
- **依赖库**：
  - `aiohttp`（用于 HTTP 请求）
- **核心 API**：AstrBot 插件 API

### 3.2 数据结构

| 数据结构 | 描述 | 存储方式 |
|---------|------|----------|
| 用户令牌 | 存储用户登录"你的日记"后的令牌 | AstrBot KV 存储 |
| 对话缓冲区 | 临时存储用户与 AI 的对话记录 | 内存 |

### 3.3 API 接口

| API 接口 | 方法 | 功能描述 |
|---------|------|----------|
| `https://nijiweb.cn/api/login/` | POST | 用户登录，获取令牌 |
| `https://nijiweb.cn/` | GET | 获取用户信息和日记列表 |
| `https://nijiweb.cn/api/` | POST | 获取日记内容、发布日记 |

## 4. 实现细节

### 4.1 插件注册
- 使用 `@register` 装饰器注册插件
- 插件名称：`diary_uploader`
- 插件描述：自动上传聊天记录到日记插件

### 4.2 命令处理
- `/login <邮箱> <密码>`：绑定"你的日记"账号
- `/logout`：解绑"你的日记"账号（可选）

### 4.3 事件监听
- 监听所有消息事件，记录用户消息和 AI 回复
- 仅记录文本内容，忽略非文本消息

### 4.4 调度器
- 每天北京时间 23:50 触发上传
- 遍历所有绑定用户，上传当日对话记录

### 4.5 上传逻辑
1. 检查用户是否已绑定
2. 获取当日对话记录
3. 格式化对话内容
4. 检查当天是否已有日记
5. 若有，获取原有内容并追加
6. 发布日记
7. 上传成功后清空当日记录

## 5. 验收标准

| 验收项 | 测试方法 | 预期结果 |
|-------|---------|----------|
| 账号绑定 | 发送 `/login <邮箱> <密码>` | 返回绑定成功提示 |
| 对话记录 | 发送多条消息，查看缓冲区 | 消息被正确记录 |
| 自动上传 | 等待 23:50 或手动触发 | 对话被上传至"你的日记" |
| 多用户支持 | 多个用户绑定账号并发送消息 | 各自的对话被独立记录和上传 |
| 日记追加 | 一天内多次发送消息 | 所有消息被追加到同一篇日记 |

## 6. 风险评估

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| 网络异常 | 上传失败 | 数据保留，次日重试 |
| 插件重启 | 未上传数据丢失 | 建议避免在 23:50 前频繁重启 |
| 账号密码错误 | 绑定失败 | 返回错误提示，引导用户检查 |

## 7. 开发计划

1. **需求分析**：确认功能需求和技术要求
2. **代码重写**：根据需求文档重写插件代码
3. **测试验证**：测试所有功能是否正常工作
4. **文档更新**：更新 README.md 和相关文档
5. **部署发布**：提供安装和使用说明

## 8. 总结

Niji Diary Logger 插件旨在为用户提供一个自动、安全、便捷的 AI 对话存档解决方案。通过每日自动同步，用户可以轻松回顾与 AI 的所有对话，打造属于自己的私人 AI 对话历史库。插件设计简洁，功能明确，易于使用和维护。